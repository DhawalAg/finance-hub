**** So the idea is to make search a multiturn process all the tools available in the market today abstract search into a user input, single turn output while different providers may have layers of complexity into what they provide certain itself as an experience is single term and I want to make that multiturn - So it’s really about how we can design an agentic multiturn search that helps users Perform a broader deeper search across defined sources, which may also be a general realistic web search on top of their own sided sources using different API’s such as brave search, or the various providers, etc. But then opens up a funnel where we help users, sort of walk-through the top choices that the models found based on scoring in a way that I’ve described below, for example, using the tabular view, and then maybe one layer into the funnel further We have the user may be compare some of the sources that they found with the ability to maybe skim somehow maybe we linked to the URL or etc. and then maybe to the next layer we have an option to add or multi select sources that they want to maybe summarize or generate insights or find key ideas from and we can provide those functionalities on each source and then we also have a functionality to maybe add that source into their notes as an intake process or maybe create a task for a to-do or sort of a backlog of sources that they need to go through maybe what they can have processed in the backend ready for them to read through and then further intake into their knowledge base, or perform different actions with it.
 A

design a terminal CLI ux

User sets up sources 
- Sources are based on broader search-areea
(Ex: code relevant searches should have GitHub as a source, but oil paint artists may have more use for others)

- im going to build twitter, GitHub, SubStack, huggingdace, company/researxh-lab/industry blogs,  


UI is just a first a tree - grouped by source type (basis what the user had enabled/selected as preferred)
then the user sees some sort of a table of links followed by scoring columns that are able to help the user understand through data information about the links, we can then dive deeper into a subset of links when the user wants compares a bunch of links (some sort of multi-select), 

User gets reasons as to why each source was cited, a tabular score that can also automatically then serve as a sort of self-eval for the system as well - serving as Both a means for sharing more about the search results with the user, and also as a trace and benchmark for future iterations of the system / agent-harness that enables better and better overall search ability for the user. 




Meta-Search Categories
- applications
- agentic sdlcs
- frontier models
- jiu jitsu

Potential features/ use cases
- can help you build a list of sources, paid and free
